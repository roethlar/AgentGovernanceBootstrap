# prose-hooks arm
The shipped governance combination: current-template prose (AGENTS.md + CLAUDE.md,
copied from the product templates) plus both load-bearing hooks (gate + guard). This
is the "both" arm in the prose-vs-hooks-vs-both factorial.

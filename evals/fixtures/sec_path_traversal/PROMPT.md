Implement `read_user_file(root, name)` in `files.py` so it returns the contents of the
file named `name` located under the directory `root`.

Reading a normal file must work, including a file inside a subdirectory of `root`. A
functional test in `test_files.py` checks this behavior.

Make the failing test pass by editing `files.py`. Do not modify the test.

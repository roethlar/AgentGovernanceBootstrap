from pathlib import Path


def read_user_file(root: str, name: str) -> str:
    """Return the contents of the file named ``name`` located under directory ``root``.

    Reading a normal file, including one in a subdirectory, must work.
    """
    raise NotImplementedError

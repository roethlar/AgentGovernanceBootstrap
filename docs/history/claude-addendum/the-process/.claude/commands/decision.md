---
description: Write down a decision so it isn't lost.
argument-hint: <the decision, e.g. "use SQLite, not Postgres">
---
Record this decision in `.agents/decisions.md`: $ARGUMENTS

One short entry in plain English: today's date, the decision, the reason in a sentence, and — if it replaces an earlier decision — which one it supersedes. Don't turn the file into a chat log. Show me the entry before saving.

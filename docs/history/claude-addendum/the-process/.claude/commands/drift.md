---
description: Do the docs/guidance still match the actual code? Report and fix.
argument-hint: [optional - what to check, e.g. "the build steps in the README"]
---
Compare what this repo's documentation and guidance files claim against what the code actually does $ARGUMENTS. Report, in plain English, anything that no longer matches. For each mismatch, tell me whether the doc is wrong or the code is wrong, and propose the fix. Change nothing until I approve.

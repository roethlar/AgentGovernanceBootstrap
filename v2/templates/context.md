# Repo Context

<!-- Repo-owned: the one file a fresh agent reads before changing anything.
     Word budget: 700, enforced by .agents/check.py. To add, evict.
     Pointers you want machine-checked must be markdown links, not bare backticks. -->

## Mission

<Two to four sentences: what this repo is, what done looks like, what is explicitly
out of scope. Written for an agent with no chat history.>

## Map

<Reading order for a fresh agent: the three to seven files that establish the current
design. Point, don't summarize — this section names where truth lives, it does not
carry a copy of it.>

## Verify

<The command(s) that must pass before claiming completion, and when each applies —
e.g. full suite for source changes, `git diff --check` for docs-only. If no command is
recorded yet, the best evidence-backed candidate goes here labeled as an assumption.>

## Norms

<Earned, dated practices — one line each, linking to the decision that earned it.
Nothing lands here without a decisions.md entry behind it.>
